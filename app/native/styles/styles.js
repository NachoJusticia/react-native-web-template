import { StyleSheet } from 'react-native';

export const appStyle = StyleSheet.create({
  reactNativeWeb: {
    backgroundColor: '#e5edf9',
    paddingTop: 65,
    flex: 1,
    alignItems: 'center'
  }
});
